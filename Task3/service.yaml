# ClusterIP for the test app on 8080.
apiVersion: v1
kind: Service
metadata:
  name: scaletestapp
  labels:
    app: scaletestapp
spec:
  type: ClusterIP
  selector:
    app: scaletestapp
  ports:
    - name: http
      port: 8080
      targetPort: 8080
      protocol: TCP
